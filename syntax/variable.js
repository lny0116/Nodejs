var a=1; // =는 대입 연산자. 오른쪽에 있는 값을 왼쪽의 변수에다 대입하여 변수의 값을 오른쪽(상수)으로 만드는 역할.
console.log(a); // 1의 값이 나옴.
a=2; // 앞서 변수를 만들 때는 var을 꼭 붙이는 게 좋다. (이미 만든 변수는 붙이지 않아도 괜찮다.) 나중에 유효범위를 배워 깨달을 때까지.
console.log(a); // 2가 나옴.

// 이러한 주석들은 comment라고도 한다.